import { addTwoNumbers, multiplyTwoNumbers } from './math';

describe('Math', () => {
  describe('when given two numbers', () => {
    it('should add add them togehter', () => {
    });

    it('should multiply the numbers', () => {});
  });
});
