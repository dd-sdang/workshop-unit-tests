/* TDD: Test Driven Development */

import { Student } from './student';

describe('Student', () => {
  let student;
  beforeEach(() => {
    student = new Student('Mercy', 32, 'Support', 'German');
  });

  it('should create student', () => {});

  it('should update student major', () => {});
});
