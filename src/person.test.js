import { Person } from './person';

describe('Person', () => {
  let person;
  
  beforeEach(() => {
    person = new Person('Mcree', 28, 'Defense');
  });

  it('should create person', () => {});

  it('should update age', () => {});

  it('should update name', () => {});

  it('should update role', () => {});
});
