import { sortArray, multiplyArrayWithValue, combineArray } from './array';

describe('Array', () => {
  it('should sort array', () => {
    // Arrange
    // Act and Assert
  });

  it('should multiply values in array', () => {});

  it('should create one string out of array', () => {});
});
